#include <bits/stdc++.h>

using namespace std;

class A {
public:
  static void solve(istream &cin = std::cin, ostream &cout = std::cout) {
    int a, b, c;
    cin >> a >> b >> c;

    cout << "Sum of " << a << ", " << b << " and " << c << " is " << a + b + c
         << endl;
  }
};
